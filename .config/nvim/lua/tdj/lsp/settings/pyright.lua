return {
    settings = {
        --[[]
        python = {
            analysis = {
                typeCheckingMode = "strict",
                autoSearchPaths = true,
                diagnosticMode = "workspace",
                useLibaryCodeForTypes = true,
                -- extraPaths = { vim.env.VIRTUAL_ENV },
            }
        }
        ]]
    }
}
