#!/usr/bin/env bash

if [[ $# -eq 1 ]]; then
    selected=$1
else
    selected=$(tmux list-windows -F "#I:#W" | fzf-tmux | cut -d ":" -f 1)
fi

if [[ -z $selected ]]; then
    exit 0
fi

tmux select-window -t $selected
