#!/usr/bin/env bash

if [[ $# -eq 1 ]]; then
    selected=$1
else
    selected=$(tmux ls | grep : | cut -d. -f1 | awk '{print substr($1, 0, length($1)-1)}' | fzf-tmux -p --reverse)
fi

if [[ -z $selected ]]; then
    exit 0
fi

tmux switch-client -t $selected
