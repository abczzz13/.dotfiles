shell: zsh

configs:
    include:
        - ~/.kube/config
        - ~/.kube/*.yml
        - ~/.kube/*.yaml
        - ~/.kube/configs/*.yml
        - ~/.kube/configs/*.yaml
        - ~/.kube/kubie/*.yml
        - ~/.kube/kubie/*.yaml
        - ~/.kube/configs/*/config
    exclude:
        - ~/.kube/kubie.yaml

prompt:
    zsh_use_rps1: true
